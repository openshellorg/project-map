name: project-map CI

on:
  push:
    branches: [ main ]
    tags: [ "v*" ]
  pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # Layout matches local CODE_ROOT: github.com/{openshellorg,dlang-supplemental}/...
      # so lib/dub.json path ../../../dlang-supplemental/json5-dlang resolves.
      - name: Checkout project-map
        uses: actions/checkout@v4
        with:
          path: openshellorg/project-map

      - name: Checkout json5-dlang (path dependency)
        uses: actions/checkout@v4
        with:
          repository: dlang-supplemental/json5-dlang
          path: dlang-supplemental/json5-dlang

      - name: Set up D (LDC)
        uses: dlang-community/setup-dlang@v1
        with:
          compiler: ldc-latest

      - name: Build library (project-map)
        working-directory: openshellorg/project-map/lib
        run: dub build --build=release

      - name: Build CLI (lsgrouped)
        working-directory: openshellorg/project-map/apps/lsgrouped
        run: dub build --build=release
